# Le scanner actuel tourne avec la stdlib seule (urllib + gzip).
# requests est optionnel mais pratique si tu refactores les appels HTTP.
requests>=2.31

# Dashboard web (dashboard.py) :
streamlit>=1.33
pandas>=2.2
plotly>=5.20
